[package]
name = "detguest-sdk"
description = "In-guest workload instrumentation API for the deterministic-execution platform"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
detguest-wire = { path = "../detguest-wire" }
